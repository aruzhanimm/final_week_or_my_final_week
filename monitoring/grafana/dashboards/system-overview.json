{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "timeseries",
      "title": "Gateway Request Rate",
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "targets": [{"expr": "sum(rate(gateway_http_requests_total[1m]))"}]
    },
    {
      "type": "timeseries",
      "title": "Gateway P95 Latency",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(gateway_http_request_duration_seconds_bucket[5m])) by (le))"}]
    },
    {
      "type": "timeseries",
      "title": "Gateway Error Rate",
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
      "targets": [{"expr": "sum(rate(gateway_http_requests_total{status=~\"5..\"}[5m])) / sum(rate(gateway_http_requests_total[5m]))"}]
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["sre", "microservices"],
  "templating": {"list": []},
  "time": {"from": "now-15m", "to": "now"},
  "title": "SRE Microservices Overview",
  "uid": "sre-overview",
  "version": 1
}
